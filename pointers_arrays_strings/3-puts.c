#include "main.h"

/**
 * _puts - Exercise #3 function
 * @str: The string to print its content
 * Description: rints a string, followed by a
 * new line, to stdout
 *
 * Return: void
*/

void _puts(char *str)
{
	puts(str);
}
