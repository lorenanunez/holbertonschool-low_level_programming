#include "main.h"

/**
 * _strlen - Exercise #3 function
 * @s: The string to return the lenght of
 * Description: returns the length of a string
 *
 * Return: The length of the string
*/

int _strlen(char *s)
{
	return (strlen(s));
}
